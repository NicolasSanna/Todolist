@model Todolist.Models.Theme

@{
    ViewData["Title"] = "Détails";
}

<h1>@ViewData["Title"]</h1>

<div>
    <h4>Theme</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.ThemeId">Éditer</a> |
    <a asp-action="Index">Revenir à la liste</a>
</div>
